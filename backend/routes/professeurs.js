const express = require('express');const router = express.Router();const Professeur = require('../models/Professeur');const pool = require('../config/database');
router.get('/', async (req, res) => { try { const { institution_id } = req.query; let query = 'SELECT DISTINCT p.* FROM professeurs p'; const params = []; if (institution_id) { query += ' LEFT JOIN titulaires t ON p.id=t.professeur_id LEFT JOIN classes c ON t.classe_id=c.id WHERE c.institution_id=?'; params.push(institution_id); } query += ' ORDER BY p.nom, p.prenom'; const [r] = await pool.query(query, params); res.json({ success: true, data: r }); } catch(e) { res.status(500).json({ success: false, error: e.message }); } });
router.get('/:id', async (req, res) => { try { const data = await Professeur.findById(req.params.id); if (!data) return res.status(404).json({ success: false, message: 'Professeur non trouvé' }); res.json({ success: true, data }); } catch(e) { res.status(500).json({ success: false, error: e.message }); } });
router.post('/', async (req, res) => { try { const data = await Professeur.create(req.body); res.status(201).json({ success: true, data }); } catch(e) { res.status(500).json({ success: false, error: e.message }); } });
router.put('/:id', async (req, res) => { try { await Professeur.update(req.params.id, req.body); res.json({ success: true, message: 'Professeur modifié' }); } catch(e) { res.status(500).json({ success: false, error: e.message }); } });
router.delete('/:id', async (req, res) => { try { const ok = await Professeur.delete(req.params.id); if (!ok) return res.status(404).json({ success: false, message: 'Professeur non trouvé' }); res.json({ success: true, message: 'Professeur supprimé' }); } catch(e) { res.status(500).json({ success: false, error: e.message }); } });
router.post('/pointer', async (req, res) => { try { const r = await Professeur.pointer(req.body); if (!r.success) return res.json({ success: false, message: r.message }); res.json({ success: true, message: 'Pointage enregistré' }); } catch(e) { res.status(500).json({ success: false, error: e.message }); } });
router.get('/presence/:professeurId', async (req, res) => { try { const data = await Professeur.getPresenceDuJour(req.params.professeurId); res.json({ success: true, data }); } catch(e) { res.status(500).json({ success: false, error: e.message }); } });
module.exports = router;