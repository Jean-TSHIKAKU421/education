const pool = require('../config/database');
async function rattraperPresencesManquantes() { try { const [eleves] = await pool.query('SELECT id, classe_id, date_inscription FROM eleves'); const aujourdhui = new Date().toISOString().split('T')[0]; for (const eleve of eleves) { const [presences] = await pool.query('SELECT date_presence FROM presences WHERE eleve_id=?', [eleve.id]); const datesExistantes = new Set(presences.map(p => p.date_presence.toISOString().split('T')[0])); let dateCourante = new Date(eleve.date_inscription); while (dateCourante <= new Date(aujourdhui)) { const dateStr = dateCourante.toISOString().split('T')[0]; if (!datesExistantes.has(dateStr)) { await pool.query('INSERT IGNORE INTO presences (eleve_id, date_presence, statut, methode_pointage) VALUES (?,?,?,?)', [eleve.id, dateStr, 'absent', 'AUTO']); } dateCourante.setDate(dateCourante.getDate() + 1); } } console.log('Rattrapage des présences manquantes terminé'); } catch(e) { console.error('Erreur rattrapage:', e.message); } }
module.exports = { rattraperPresencesManquantes };