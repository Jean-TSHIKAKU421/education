class ImportExcelUI {
    constructor(options = {}) { this.service = importExcelService; this.institutionId = options.institutionId || 1; this.classeId = options.classeId || null; this.classeNom = options.classeNom || ''; this.onComplete = options.onComplete || (() => {}); }
    async lancerImport(file) { try { const eleves = await this.service.lireFichier(file); if (!eleves.length) { this.ouvrirAlert('Aucun élève', 'Aucun élève trouvé dans le fichier.', 'warning'); return; } await this.service.chargerContexte(this.institutionId); const optionsInconnues = this.service.verifierOptionsInconnues(); let listeFinale = eleves; if (optionsInconnues.length > 0) { const resultat = await this.afficherModalOptionsInconnues(optionsInconnues); if (resultat === 'cancel') return; await this.service.chargerContexte(this.institutionId); listeFinale = eleves; } const avecStatut = listeFinale.map(e => { const np = String(e.nom || '').toLowerCase().trim(), p = String(e.prenom || '').toLowerCase().trim(); return { ...e, doublon: this.service.existants.some(ex => ex.nom === np && ex.prenom === p) }; }); const nouveaux = avecStatut.filter(e => !e.doublon); if (!nouveaux.length) { this.ouvrirAlert('Information', 'Tous les élèves existent déjà.', 'info'); return; } const ok = await this.afficherModalApercu(avecStatut, nouveaux, listeFinale.length); if (!ok) return; await this.executerImport(nouveaux); if (this.onComplete) this.onComplete(); } catch(ex) { this.ouvrirAlert('Erreur', ex.message || ex, 'error'); } }
    afficherModalOptionsInconnues(optionsInconnues) { return new Promise((resolve) => { const overlay = document.createElement('div'); overlay.className = 'modal-overlay'; overlay.id = 'modal-options-inc'; overlay.onclick = e => { if (e.target === overlay) { overlay.remove(); resolve('cancel'); } }; const lignes = optionsInconnues.map(o => `<tr><td><strong>${o.nom}</strong></td><td>${o.niveau}</td><td>${o.count} élève(s)</td></tr>`).join(''); overlay.innerHTML = `<div class="modal" style="max-width:550px" onclick="event.stopPropagation()"><div class="modal-header"><h3><i class="fas fa-exclamation-triangle" style="color:var(--warning)"></i> Options inconnues</h3><button class="modal-close" id="close-options-inc"><i class="fas fa-times"></i></button></div><div class="modal-body"><p style="color:var(--text-secondary);margin-bottom:1rem">Ces options n'existent pas encore :</p><div class="table-container"><table class="data-table"><thead><tr><th>Option</th><th>Niveau</th><th>Élèves</th></tr></thead><tbody>${lignes}</tbody></table></div><p style="color:var(--text-muted);margin-top:1rem">Voulez-vous créer ces options ?</p></div><div class="modal-footer"><button class="btn btn-ghost" id="btn-ignorer-options-inc"><i class="fas fa-times"></i> Ignorer</button><button class="btn btn-primary" id="btn-creer-options-inc"><i class="fas fa-check"></i> Créer</button></div></div>`; document.body.appendChild(overlay); document.getElementById('close-options-inc').onclick = () => { overlay.remove(); resolve('cancel'); }; document.getElementById('btn-ignorer-options-inc').onclick = () => { overlay.remove(); resolve('cancel'); }; document.getElementById('btn-creer-options-inc').onclick = async () => { overlay.remove(); for (const opt of optionsInconnues) { try { await apiPost('/classes/option/secondaire', { code: opt.code, nom: opt.nom, institution_id: this.institutionId }); } catch(e) {} } this.ouvrirAlert('Succès', `${optionsInconnues.length} option(s) créée(s) avec succès !`, 'success'); setTimeout(() => resolve('created'), 500); }; }); }
    afficherModalApercu(avecStatut, nouveaux, total) { return new Promise((resolve) => { const overlay = document.createElement('div'); overlay.className = 'modal-overlay'; overlay.id = 'modal-apercu-import'; overlay.onclick = e => { if (e.target === overlay) { overlay.remove(); resolve(false); } }; const lignes = avecStatut.slice(0, 100).map((e, i) => { const badge = e.doublon ? '<span class="badge badge-warning"><i class="fas fa-copy"></i> Existe déjà</span>' : '<span class="badge badge-success"><i class="fas fa-plus-circle"></i> Nouveau</span>'; return `<tr style="${e.doublon ? 'opacity:0.5' : ''}"><td>${i + 1}</td><td>${e.nom || ''}</td><td>${e.prenom || ''}</td><td>${String(e.genre || '').toUpperCase().charAt(0)}</td><td>${e.dateNaissance || ''}</td><td>${e.classe || ''}${e.option ? ' - ' + e.option : ''}</td><td>${badge}</td></tr>`; }).join(''); overlay.innerHTML = `<div class="modal" style="max-width:750px;max-height:80vh;display:flex;flex-direction:column" onclick="event.stopPropagation()"><div class="modal-header"><h3><i class="fas fa-list"></i> Aperçu (${total} élèves)</h3><button class="modal-close" id="close-apercu-import"><i class="fas fa-times"></i></button></div><div class="modal-body" style="flex:1;overflow-y:auto;padding-bottom:0.5rem"><p style="color:var(--text-muted);font-size:0.8rem;margin-bottom:1rem"><span class="badge badge-success"><i class="fas fa-plus-circle"></i> ${nouveaux.length} nouveaux</span> <span class="badge badge-warning"><i class="fas fa-copy"></i> ${avecStatut.length - nouveaux.length} déjà existants</span></p><div class="table-container"><table class="data-table"><thead><tr><th>#</th><th>Nom & Postnom</th><th>Prénom</th><th>G</th><th>Date naiss.</th><th>Classe</th><th>Statut</th></tr></thead><tbody>${lignes}</tbody></table></div></div><div class="modal-footer"><button class="btn btn-ghost" id="btn-annuler-import"><i class="fas fa-times"></i> Annuler</button><button class="btn btn-primary" id="btn-lancer-import" ${nouveaux.length === 0 ? 'disabled' : ''}><i class="fas fa-upload"></i> Importer ${nouveaux.length} nouveaux</button></div></div>`; document.body.appendChild(overlay); document.getElementById('close-apercu-import').onclick = () => { overlay.remove(); resolve(false); }; document.getElementById('btn-annuler-import').onclick = () => { overlay.remove(); resolve(false); }; document.getElementById('btn-lancer-import').onclick = () => { overlay.remove(); resolve(true); }; }); }
    async executerImport(eleves) { const overlay = document.createElement('div'); overlay.className = 'modal-overlay'; overlay.id = 'modal-progression'; overlay.innerHTML = `<div class="modal" style="max-width:400px;text-align:center" onclick="event.stopPropagation()"><div class="modal-body" style="padding:2rem"><div class="spinner"></div><p style="margin-top:1rem">Importation... <span id="import-count">0</span> / ${eleves.length}</p></div></div>`; document.body.appendChild(overlay); let success = 0, errors = 0; const processNext = (i) => { if (i >= eleves.length) { overlay.innerHTML = `<div class="modal" style="max-width:400px;text-align:center" onclick="event.stopPropagation()"><div class="modal-body" style="padding:2rem"><i class="fas fa-check-circle" style="font-size:3rem;color:var(--success);margin-bottom:0.5rem;display:block"></i><h3>Import terminé</h3><p style="color:var(--text-secondary)">${success} élèves importés${errors > 0 ? ` · ${errors} erreurs` : ''}</p><button class="btn btn-primary" style="margin-top:1.25rem;width:100%" onclick="document.getElementById('modal-progression').remove()"><i class="fas fa-check"></i> OK</button></div></div>`; return; } const e = eleves[i]; const nomPostnom = String(e.nom || '').trim(), prenom = String(e.prenom || '').trim(), date_naissance = String(e.dateNaissance || ''), genre = (String(e.genre || 'M').trim().toUpperCase().charAt(0) === 'F') ? 'F' : 'M', adresse = String(e.adresse || '').trim(), classe_nom = String(e.classe || '').trim(), option_nom = String(e.option || '').trim(); const classe_id = this.service.trouverClasseId(classe_nom, option_nom) || this.classeId; if (!classe_id) { errors++; document.getElementById('import-count').textContent = i + 1; requestAnimationFrame(() => processNext(i + 1)); return; } API.createEleve({ nom: nomPostnom, prenom, date_naissance, genre, adresse, classe_id }).then(r => { if (r && r.success) success++; else errors++; }).catch(() => errors++).finally(() => { document.getElementById('import-count').textContent = i + 1; requestAnimationFrame(() => processNext(i + 1)); }); }; processNext(0); }
    ouvrirAlert(titre, message, type) { const overlay = document.createElement('div'); overlay.className = 'modal-overlay'; overlay.style.zIndex = '2000'; overlay.id = 'alert-' + Date.now(); overlay.onclick = e => { if (e.target === overlay) overlay.remove(); }; const icones = { error: 'exclamation-circle', success: 'check-circle', warning: 'exclamation-triangle', info: 'info-circle' }; const couleurs = { error: 'var(--danger)', success: 'var(--success)', warning: 'var(--warning)', info: 'var(--info)' }; overlay.innerHTML = `<div class="modal" style="max-width:380px;text-align:center" onclick="event.stopPropagation()"><div class="modal-body" style="padding:2rem 1.5rem"><i class="fas fa-${icones[type] || 'info-circle'}" style="font-size:2.5rem;color:${couleurs[type] || 'var(--primary)'};margin-bottom:0.75rem;display:block"></i><h3 style="margin-bottom:0.5rem">${titre}</h3><p style="color:var(--text-secondary);font-size:0.9rem">${message}</p><button class="btn btn-primary" style="margin-top:1.25rem;width:100%" onclick="document.getElementById('${overlay.id}').remove()"><i class="fas fa-check"></i> OK</button></div></div>`; document.body.appendChild(overlay); }
}